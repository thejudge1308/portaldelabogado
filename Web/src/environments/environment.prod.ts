// environment.prod.ts (producción tradicional)
export const environment = {
  production: true,
  mode: 'production',
  apiUrl: '/api',
  appName: 'Portal de Abogados',
  version: '1.0.0',
  apiTimeout: 10000,
  enableLogging: false,
  enableAnalytics: true
};